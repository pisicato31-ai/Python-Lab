[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-lab"
version = "0.1.0"
description = "Projeto profissional estruturado"
authors = [{ name = "Renato Soares" }]
readme = "README.md"
requires-python = ">=3.12"

dependencies = []
